import {
    StyleSheet,
    Text,   
    View,
    TextInput,
    TouchableOpacity,
    Dimensions,
  } from 'react-native';
const styless = {
    widowWidth : Dimensions.get('window').width,
    widoHeight : Dimensions.get('window').height,
  };
  export default styless;